export interface CountryFlagProps {
    countryCode: string;
}
export interface PostInterface {
    authorId: string;
    videoId: string;
    videoLink: string;
    authorName: string;
    description: string;
}